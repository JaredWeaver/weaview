import NavBar from './Navbar'
import Footer from './Footer'
const Layout = ({children}) => {
    return (
        <div>
        <NavBar />
        <Footer/>
        </div>
    )
}

export default Layout
